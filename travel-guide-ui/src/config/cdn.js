export const CDN_BASE = 'https://cdn.jsdelivr.net/gh/Krwaby123/travel-assets-yn@main'

export const getImageUrl = (path) => {
  return `${CDN_BASE}${path}`
}

export const getMusicUrl = (path) => {
  return `${CDN_BASE}${path}`
}